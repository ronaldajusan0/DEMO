{
  "name": "sprint-demo",
  "version": "0.1.0",
  "description": "A tiny task-board library used to demo AI-assisted sprints, the auto dev loop, and git workflows.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch",
    "loop": "bash scripts/dev-loop.sh"
  },
  "license": "MIT"
}
