// SPRINT-2 FEATURE — this is the demo task.
//
// The tests in test/sprint.test.js describe what these functions must do.
// They are RED right now. During the demo you ask the AI to implement the
// functions until the auto dev loop goes green. Do not edit the tests.

const PRIORITY_ORDER = { high: 0, medium: 1, low: 2 };

// TODO(sprint-2): return board.tasks sorted by priority
//   high -> medium -> low. Tasks of equal priority keep insertion order.
export function prioritizedTasks(board) {
  throw new Error("not implemented");
}

// TODO(sprint-2): return sprint stats:
//   { total, done, remaining, percentComplete }
//   percentComplete is an integer 0..100 (done / total * 100, rounded).
//   An empty board is 100% complete.
export function sprintProgress(board) {
  throw new Error("not implemented");
}
